<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>QR Generator</title>
    <style>
        body {
            color: black;
            font-family: 'Gill Sans', 'Gill Sans MT', Calibri, 'Trebuchet MS', sans-serif;
            font-size: 30px;
        }
        h1 {
            text-align: center;
            justify-content: center;
            padding: 40px;
        }
        .form {
            text-align: center;
            justify-content: center; 
        }
        .form input {
            width: calc(50% - 22px);
            margin: 5px;
            padding: 10px;
            border: 1px solid #555;
            border-radius: 5px;
        }
        .form input::placeholder {
            font-size: 20px;
            color: #000;
        }
        .form label {
            font-size: 30px;
        }
        button {
            padding: 12px;
            margin: 40px;
            width: 130px;
            background-color: black;
            color: white;
            text-align: center;
            align-items: center;
            justify-content: center;
            border-radius: 5px;
        }
        button:hover {
            cursor: pointer;
            background-color: grey;
        }
        .code {
            width: 400px;
            height: 400px;
            border: 2px solid black;
            padding: 10px;
            margin: 10px;
        }
        .main-heading {
            font-size: 40px;
            font-weight: bold;
        }
        .form-label {
            font-size: 30px;
            font-weight: bold;
        }
        .form-input {
            width: calc(50% - 22px);
            margin: 5px;
            padding: 10px;
            border: 1px solid #555;
            border-radius: 5px;
        }
        .form-button {
            padding: 12px;
            margin: 40px;
            width: 130px;
            background-color: black;
            color: white;
            text-align: center;
            align-items: center;
            justify-content: center;
            border-radius: 5px;
        }
        .form-button:hover {
            cursor: pointer;
            background-color: grey;
        }
        .result-heading {
            font-size: 30px;
            font-weight: bold;
        }
        .qr-image {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1 class="main-heading">QR GENERATOR</h1>
    <div class="form">
        <form action="/submit" method="post">
            <label for="url" class="form-label"><b>URL:</b></label>
            <input type="text" id="url" name="url" class="form-input" placeholder="Enter your URL" required>
            <button type="submit" class="form-button">Submit</button>
        </form>
        <% if (url) { %>
            <h2 class="result-heading">Generated QR Code for: <%= url %></h2>
            <img src="/qr-img.png" alt="QR Code" class="qr-image">
        <% } %>
    </div>
</body>
</html>
